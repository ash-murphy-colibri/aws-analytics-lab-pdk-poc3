[tool.poetry]
name = "projc"
version = "0.2.7"
description = ""
authors = ["Ash Murphy <ashley.murphy@colibridigital.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.32.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
